_default_:
  _all:
    enabled: false

  properties:
    "@timestamp":
      type: date
      index: not_analyzed

    host:
      type: string
      index: analyzed

    ip:
      type: ip
      index: not_analyzed

    message:
      type: string
      index: analyzed

    facility:
      type: string
      index: analyzed

    severity:
      type: string
      index: not_analyzed

    hostname:
      type: string
      index: not_analyzed
